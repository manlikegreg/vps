# Frontend origins allowed by CORS (comma-separated)
MASTER_FRONTEND_URL=http://localhost:5174
MASTER_FRONTEND_URLS=http://localhost:5174,http://localhost:5173

# Backend config
MASTER_BACKEND_PORT=9000
AGENTS_REGISTRY_PATH=config/agents.json

# Admin auth (set via secrets manager in production)
ADMIN_USERNAME=
ADMIN_PASSWORD=
ADMIN_SECRET=
ADMIN_TOKEN_TTL=7200

# Optional: auto-run a command when a new agent connects
# e.g., AUTO_RUN_COMMAND=python -V
# AUTO_RUN_MODE=interactive|command (default interactive if not set)
AUTO_RUN_COMMAND=
AUTO_RUN_MODE=interactive
