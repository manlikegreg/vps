# Backend environment configuration
# Credentials (development defaults; change in production)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin

# Token TTL (seconds)
TOKEN_TTL_SECONDS=86400

# Frontend origin allowed for CORS
FRONTEND_URL=http://localhost:5173

# Master control connection
# URL is now hardcoded in backend/main.py

# Optional: TLS certificate pinning for master WebSocket (WSS)
# Set either MASTER_CERT_SHA256 to the SHA256 fingerprint of the master's leaf cert (hex, no colons)
# OR set MASTER_CA_PEM_PATH to a PEM file path containing a trusted CA/leaf cert.
# Leave empty to use system CA trust (no pinning).
MASTER_CERT_SHA256=
MASTER_CA_PEM_PATH=

# Audio recording (agent-side)
AUDIO_ENABLED=1
AUDIO_SAMPLE_RATE=44100
AUDIO_CHANNELS=1
# Safety cap to auto-stop if not stopped explicitly (seconds)
AUDIO_MAX_SECONDS=300

# Agent HTTP base (for master proxy)
AGENT_HTTP_BASE=http://localhost:8000

CAMERA_ENABLED=1
